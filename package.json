{
  "name": "ractive",
  "version": "1.0.0-edge",
  "license": "MIT",
  "private": true,
  "engines": {
    "node": ">=4.0.0",
    "npm": ">=2.14.2"
  },
  "devDependencies": {
    "karma": "^1.7.1",
    "karma-failed-reporter": "0.0.3",
    "karma-phantomjs-launcher": "^1.0.4",
    "karma-qunit": "^1.2.1",
    "phantomjs-prebuilt": "^2.1.15",
    "qunitjs": "^2.4.0",
    "rollup": "^0.50.0",
    "rollup-plugin-buble": "^0.15.0",
    "standard": "^10.0.3"
  },
  "scripts": {
    "start": "npm run dev",
    "dev": "gobble --env=dev serve",
    "build": "gobble --env=build build .tmp -f && cp -r .tmp/* packages && rm -rfv .tmp"
  }
}
